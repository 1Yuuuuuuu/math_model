interface:
  display_name: "CUMCM Paper Reviewer"
  short_description: "Score and review paper quality without editing the source"
  default_prompt: "Use $paper-reviewer to independently score and review this CUMCM paper package."
policy:
  allow_implicit_invocation: true
