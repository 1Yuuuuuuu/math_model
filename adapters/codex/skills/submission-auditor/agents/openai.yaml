interface:
  display_name: "CUMCM Submission Auditor"
  short_description: "Audit the exact final source and PDF against hard gates"
  default_prompt: "Use $submission-auditor to audit this exact final CUMCM source and PDF package."
policy:
  allow_implicit_invocation: true
