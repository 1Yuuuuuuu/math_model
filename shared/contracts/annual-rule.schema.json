{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cumcm.local/contracts/annual-rule.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "rule_set_id", "year", "source_url", "verified_at", "items"],
  "properties": {
    "schema_version": {"const": "1.0"},
    "rule_set_id": {"type": "string", "pattern": "^cumcm-[a-z0-9][a-z0-9-]{2,63}(?![\\s\\S])"},
    "year": {"type": "integer", "minimum": 2000},
    "source_url": {
      "allOf": [
        {"type": "string", "format": "uri"},
        {"type": "string", "format": "cumcm-http-url"}
      ]
    },
    "verified_at": {"type": "string", "format": "date-time"},
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["rule_id", "description", "enforcement", "blocking"],
        "properties": {
          "rule_id": {"type": "string", "pattern": "^rule_[a-z0-9][a-z0-9_-]{2,63}(?![\\s\\S])"},
          "description": {"type": "string", "minLength": 1},
          "enforcement": {"enum": ["machine", "human"]},
          "blocking": {"type": "boolean"}
        }
      }
    }
  }
}
